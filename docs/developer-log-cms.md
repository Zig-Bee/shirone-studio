# CMS 开发日志

每个批次追加一条：日期、版本批次、功能与目的、页面影响、验证和发布状态。凭据不写入本日志。

## 2026-09-05 · v0.2.0 / B01 · 个人站点管理

- **功能与目的：**新增横幅、建站日期、草稿预览、音乐、公告、侧栏与导航表单；头像及封面改为图片选择器。让日常维护通过 CMS 完成。
- **页面影响：**表单分别保存到内容仓 `config/*.yaml`；标题与简介可留空跟随站点资料。音乐未配置时关闭，移除导航项目只隐藏入口。
- **资源与状态：**新增配置中的图片／音频引用检查、日期／链接／歌单校验；工作台提示本地未提交修改，明确“保存”与“发布成功”的区别。云端不伪报本地检查或部署状态。
- **内容整理：**增加“模板示例”标记和筛选；23 篇文章、6 条说说保留为草稿，个人测试文章的原草稿状态保持不变。
- **验证：**8 项 Studio 测试通过；24 篇文章、6 条说说、70 个媒体引用检查通过。新增表单的浏览器保存验证另见批次补记。
- **发布：**本地开发批次，尚未推送 GitHub Pages；现有线上后台不会自动获得本地改动。

入口关系见 [模块映射](module-map.md)。

### B01 验证补记

- 工作台真实点击“检查设置中的图片与音频”，正确列出头像和两张横幅的引用字段；检查结果不会被自动刷新状态覆盖。
- `npm run check` 8 项通过，`npm run check:content` 通过，`npm run build` 通过。
- 当前机器 npm 仓库 DNS 不可用；本地复用了已安装的同版本 `yaml@2.9.0`。依赖版本已固定，CI 安装步骤已补齐。
- CMS 新表单已在浏览器加载，包含建站日期、时区及草稿预览字段。账户菜单显示 Zig-Bee，表单读取到远程旧资料（Shirone），因此没有执行保存；本地文件夹自动选择失败，本地保存／重新打开仍待选择 `/Users/huyan/Code/shirone-content` 后验证。代理未使用所提供的令牌，未向远端仓库保存。

## 2026-09-05 · v0.2.0 / B02 · 本地保存与预览一致性

- **功能与目的：**Studio 本地服务启动时自动监听内容仓，保存后串行更新预览副本、离线图标和说说缩略图；连续保存合并处理，同步中再次保存会补跑。
- **页面影响：**工作台每 5 秒刷新等待／更新中／已更新／失败状态；本地未提交修改不阻止预览。停止 Studio 服务即停止监听。
- **边界：**仅内容仓到主题副本单向更新，不反写生成文件，不自动提交、推送或拉取 GitHub；远程 CMS 保存后仍须检查部署。
- **验证：**10 项 Studio 测试通过，覆盖连续保存串行执行及失败恢复；实际内容文件保存触发更新已核验。

## 2026-09-05 · B01/B02 发布进度

- 内容仓已提交并推送至 `0785a84`，合并远程 CMS 对测试文章的模板标记；空歌单继续关闭。
- Studio 本地提交 `78d5615` 已完成，推送收到 GitHub 403：现有令牌无此仓写权限；SSH 备用连接也未认证成功。线上后台尚未更新。
- 当前账户没有博客主题部署仓；主题源的 origin 指向模板作者，未向其推送。新部署仓名称待确定。

## 2026-09-05 · v0.2.0 / B03 · 中国标准时间

- **功能与目的：**文章精确发布时间、精确更新时间和说说发布时间固定按 `Asia/Shanghai` 输入并保存时区偏移；草稿提示明确说明不会显示在网站。
- **兼容修复：**两篇由旧云端表单生成的无时区文章时间补为 `+08:00`；内容校验拒绝新的无时区精确时间。
- **验证：**Studio 11 项测试、内容检查和构建通过；Astro 检查 0 错误。浏览器确认文章关闭草稿后首页正常显示，统计为 1 篇，不再触发跨日错误。
- **发布边界：**内容时间修复可推送内容仓；新的云端表单需 Studio 仓更新后生效。

## 2026-09-05 · v0.2.0 / B04 · 基础设置契约修复

- **功能与目的：**音乐来源增加“内容仓内置歌曲”并设为默认，说明启用条件；资源检查同时识别 CMS 保存的 `/assets/images/...` 与 `assets/images/...`。
- **页面影响：**打开播放器并选择内置歌曲后可直接显示；头像、横幅和歌曲封面上传后的路径能被前端图片管线读取。
- **契约范围：**增加表单枚举测试，覆盖音乐、语言、背景模式和侧栏参数；模块映射明确列出尚未开放的 Shirone 高级参数。
